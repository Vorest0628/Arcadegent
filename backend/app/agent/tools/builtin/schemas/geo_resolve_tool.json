{
  "name": "geo_resolve_tool",
  "kind": "function",
  "description": "Resolve map provider by province code.",
  "executor": "app.agent.tools.builtin.executors.geo_resolve:execute",
  "capabilities": ["builtin", "read_only", "geo"],
  "metadata": {
    "tool_function": "app.agent.tools.builtin.executors.geo_resolve:execute"
  },
  "input_schema": {
    "additionalProperties": false,
    "properties": {
      "province_code": {
        "anyOf": [{ "type": "string" }, { "type": "null" }],
        "title": "Province Code"
      }
    },
    "required": ["province_code"],
    "title": "GeoResolveArgs",
    "type": "object"
  }
}
